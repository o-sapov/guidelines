<p xmlns="http://www.tei-c.org/ns/1.0">To define the MIDI resolution of a score, the<hi rend="bold">@ppq</hi>attribute may be used on the <gi scheme="MEI">scoreDef</gi> element. This value can be used to interpret the values found in the<hi rend="bold">@dur.ppq</hi>attribute on elements in the <ident type="class">att.duration.gestural</ident> class.  The<hi rend="bold">@ppq</hi>attribute is also available on the <gi scheme="MEI">staffDef</gi> element in order to aid in the conversion to MEI from other representations that allow a different time base for each staff. However, these independent values for<hi rend="bold">@ppq</hi>are only interpretable in terms of a common time base. Therefore, the<hi rend="bold">@ppq</hi>attribute is required on <gi scheme="MEI">scoreDef</gi> when the values of<hi rend="bold">@ppq</hi>on the staff definitions differ. In the following example, the values of the<hi rend="bold">@ppq</hi>attributes on the <gi scheme="MEI">staffDef</gi> elements are all factors of the value of<hi rend="bold">@ppq</hi>attached to <gi scheme="MEI">scoreDef</gi>. </p>
