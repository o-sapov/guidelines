<p xmlns="http://www.tei-c.org/ns/1.0">Addresses may be encoded using the <gi scheme="MEI">address</gi> element, which itself may hold an arbitrary number of <gi scheme="MEI">addrLine</gi> elements. <specList>
      <specDesc key="address"/>
   </specList>
   <specList>
      <specDesc key="addrLine"/>
   </specList> It is important to note that the <gi scheme="MEI">address</gi> element does not hold a reference to the person or organization whose address is specified. This must be provided in a separate element, as in the following example: </p>
